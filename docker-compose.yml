version: "3.8"

services:
  backend:
    build:
      context: ./back
      dockerfile: Dockerfile
    ports:
      - "4000:4000"

  frontend:
    build:
      context: ./front
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
